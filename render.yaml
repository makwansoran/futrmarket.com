services:
  - type: web
    name: futrmarket-api
    env: node
    buildCommand: npm install
    startCommand: npm run server
    envVars:
      - key: NODE_ENV
        value: production
    # Optional: Add persistent disk for data storage (recommended for production)
    # Uncomment and configure if you need persistent storage
    # disk:
    #   name: futrmarket-data
    #   mountPath: /opt/render/project/src/data
    #   sizeGB: 1

